[project]
name = "quantum-101"
version = "0.1.0"
description = "Single-qubit gate simulator project"
readme = "readme.txt"
requires-python = ">=3.9"
dependencies = [
    "numpy",
]

[tool.uv]
package = false
